#ifndef TRIG_H
#define TRIG_H
class Trigonometry
{
    public:
        Trigonometry();
        double Sine(double angle) const;
        double Cosine(double angle)const;
        double Tangent(double angle)const;
        double exponent(double x)const;
        double logs(double y)const;
        double logs10(double z)const;
};
#endif 